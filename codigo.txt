programa {
    // Declaração de variáveis para o cálculo
    inteiro dividendo, divisor, resultado, resultadoErrado;

    dividendo = 100;  // Define o valor inicial
    divisor = 2;

    // A operação de divisão deve ser reconhecida corretamente
    resultado = dividendo / divisor;

    // Exemplo de erro
    resultadoErrado = didendo @ divisor; 

    escreva resultado; // Mostra o valor final na tela
}
